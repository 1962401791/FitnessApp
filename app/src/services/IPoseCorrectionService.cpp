/**
 * @file IPoseCorrectionService.cpp
 * @brief Ensures moc is generated for IPoseCorrectionService (abstract interface with Q_OBJECT).
 * Without a .cpp that includes this header, the linker gets undefined symbols for vtable, signals, etc.
 */
#include "services/IPoseCorrectionService.h"
